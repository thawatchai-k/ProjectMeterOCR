# รายงานสรุปผลการดำเนินงานโครงการพัฒนาระบบ OCR สำหรับมิเตอร์ไฟฟ้า

## 5.1 สรุปผลการดำเนินงาน
- **การพัฒนา OCR Engine v7.0**: ประสบความสำเร็จในการพัฒนาระบบอ่านค่ามิเตอร์ไฟฟ้า (Meter Reading) และหมายเลขเครื่อง (Serial Number) โดยใช้เทคนิคการตรวจจับจุดอ้างอิง (Anchors) ได้แก่ `/5A`, `/10A`, `/15A` และ `No.`
- **การปรับปรุงความแม่นยำ**: 
  - เพิ่มระบบ **Auto-rotation** เพื่อหมุนภาพให้อยู่ในแนวตรงโดยอัตโนมัติก่อนการประมวลผล
  - พัฒนาเทคนิค **Image Pre-processing (Black on White)** โดยการขยายภาพ 3 เท่า, ลด Noise ด้วย Gaussian Blur และใช้ Adaptive Thresholding เพื่อแยกตัวเลขออกจากพื้นหลังให้ชัดเจนที่สุด
  - เพิ่มระบบตรวจสอบความถูกต้องของข้อมูล (Validation) โดยบังคับให้ค่ามิเตอร์เป็นตัวเลข 4 หลัก และหมายเลขเครื่องเป็นตัวเลข 7 หลัก
- **โครงสร้างระบบ**: ระบบทำงานผ่าน Flask Web Service รองรับการประมวลผลผ่านไฟล์รูปภาพและแสดงผลลัพธ์เป็น JSON

## 5.2 ปัญหาที่พบในการดำเนินงาน
- **ความหลากหลายของสภาพแสง**: รูปภาพที่ถ่ายในที่มืดหรือมีแสงสะท้อนทำให้ Tesseract ตรวจจับจุดอ้างอิงผิดพลาดหรือไม่พบจุดอ้างอิง
- **ความเอียงของรูปภาพ**: หากรูปภาพมีความเอียงมากเกินไป ระบบตรวจจับจุดอ้างอิง (Anchors) จะทำงานได้ไม่เสถียร
- **Noise ในรูปภาพ**: พื้นผิวของหม้อแปลงไฟฟ้าที่มีความมันวาวหรือเงาสะท้อนทำให้เกิดจุดรบกวนในรูปภาพ ส่งผลให้อ่านตัวเลขผิดพลาด (เช่น เลข 0 อ่านเป็น 8 หรือเลข 1 อ่านเป็น 7)
- **ตำแหน่งของ Serial Number**: ในบางรุ่น ตำแหน่งของหมายเลขเครื่องอาจไม่ได้อยู่ทางขวาของคำว่า "No." เสมอไป หรือขนาดฟอนต์มีความแตกต่างกันมาก

## 5.3 แนวทางในการแก้ไขปัญหา
- **การใช้ CLAHE (Contrast Limited Adaptive Histogram Equalization)**: นำมาใช้เพื่อปรับความคมชัดของภาพในระดับ Global และ Local ช่วยให้มองเห็นตัวหนังสือได้ชัดเจนขึ้นแม้ในสภาพแสงที่ไม่เหมาะสม
- **Multiple Thresholding Strategies**: ใช้การประมวลผลภาพหลายรูปแบบพร้อมกัน (Otsu, Fixed Threshold, Adaptive Threshold) และส่งเข้าไปอ่านผลลัพธ์หลายครั้งเพื่อเลือกค่าที่ "ดีที่สุด" หรือ "พบซ้ำมากที่สุด" (Majority Vote)
- **Anchor Scaling**: ปรับขนาดภาพเป็น 2 เท่าก่อนการ Scan หาจุดอ้างอิง เพื่อให้ Tesseract สามารถระบุข้อความขนาดเล็กได้ดีขึ้น
- **Constraint-based Filtering**: ใช้ Regex และการตรวจสอบความยาวของ String (4 หลักสำหรับ Reading, 7 หลักสำหรับ S/N) เพื่อคัดกรองผลลัพธ์ที่ผิดพลาดออกไป

## 5.4 แนวทางในการพัฒนาในอนาคต
- **Implement Deep Learning Model**: เปลี่ยนจากการใช้ Tesseract (Rule-based/OCR) มาเป็นโมเดล Deep Learning เช่น YOLO หรือ CRNN สำหรับการตรวจจับตัวเลขโดยเฉพาะ เพื่อความแม่นยำที่สูงขึ้นในสภาพแวดล้อมที่ซับซับซ้อน
- **Mobile Friendly Interface**: พัฒนาส่วนหน้าบ้าน (Mobile Web/App) ให้มีกรอบนำสายตา (Overlay Guider) เพื่อช่วยให้ผู้ใช้ถ่ายภาพในมุมและระยะที่เหมาะสมที่สุด
- **Performance Optimization**: ปรับปรุงความเร็วในการประมวลผลภาพ โดยการใช้ GPU หรือปรับลดขั้นตอนการ Pre-processing ที่ไม่จำเป็น
- **Cloud Logging & monitoring**: ระบบบันทึกภาพตัวอย่างที่อ่านผิด (Error logs) เพื่อนำมาใช้ในการ Fine-tune โมเดลในอนาคต
